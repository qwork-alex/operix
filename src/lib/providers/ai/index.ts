/**
 * AI domain — adapters register here. Current LLM bridge lives in
 * `src/lib/agentLLM.ts`; it can migrate to a provider without consumer
 * changes once additional models are wired in.
 */
export * from "./types";
